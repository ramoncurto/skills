{
  "$doc": "Example only. Copy it, replace every example URL and command with authorized project evidence before execution. Diagnostics observe; eligibility and guardrails fail closed.",
  "schemaVersion": 1,
  "exampleOnly": true,
  "base": "https://example.invalid",
  "out": ".agents/runs/seo-scorecard",
  "timeoutSec": 20,
  "sitemapIndex": "/sitemap.xml",
  "maxSitemapFiles": 20,
  "samples": [
    { "path": "/", "template": "home", "expectedIndexable": true },
    { "path": "/example-content", "template": "content", "expectedIndexable": true }
  ],
  "thresholds": {
    "$note": "Record project-specific measurements as diagnostics; do not use generic word-count or payload thresholds as universal eligibility rules.",
    "default": { "maxRawKB": 300, "minTextCharacters": 500 }
  },
  "staticChecks": [
    {
      "$note": "Replace with an authorized project command. This intentionally has no executable placeholder.",
      "name": "replace with a project-specific eligibility check",
      "cmd": "false",
      "class": "eligibility"
    }
  ],
  "weights": { "live": 1, "static": 1, "guardrail": 5 }
}
